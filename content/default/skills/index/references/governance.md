# Default Skill governance

The index routes callers to the bundled Skills by stable slug. Deployments seed each
`SKILL.md` as immutable version one and preserve any current version that already exists.
Supporting references are bundle documentation; they are validated and packaged but are
not stored as part of the executable SkillVersion document.
