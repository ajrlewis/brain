[project]
name = "cortex-api"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "fastapi>=0.116,<1",
  "uvicorn[standard]>=0.35,<1",
]

[project.scripts]
cortex-api = "cortex_api.main:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
