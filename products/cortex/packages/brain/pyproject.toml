[project]
name = "cortex-brain"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["httpx>=0.28,<1", "pydantic>=2.11,<3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
