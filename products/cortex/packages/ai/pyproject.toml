[project]
name = "cortex-ai"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["openai>=1.66,<2", "pydantic>=2.11,<3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
