[project]
name = "brain-schemas"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["pydantic>=2.11,<3", "pyyaml>=6.0,<7"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
