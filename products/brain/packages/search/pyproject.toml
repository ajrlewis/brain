[project]
name = "brain-search"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "pgvector>=0.4,<1",
  "sqlalchemy>=2.0,<3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
