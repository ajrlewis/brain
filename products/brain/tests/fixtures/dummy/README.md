# Dummy content fixture

This deliberately tiny, provider-neutral fixture is test content only. It is neither a
production default bundle nor a database seed and contains no customer/example-company data,
external provider assumptions, or binary assets. Unit and transport tests may read its
manifest and exact UTF-8 documents directly.
