[project]
name = "brain-core"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "brain-schemas",
  "pydantic-settings>=2.10,<3",
]

[tool.uv.sources]
brain-schemas = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
